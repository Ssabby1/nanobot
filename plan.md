# 基于 nanobot 二次开发的健身训练与饮食协同 Agent MVP 计划

## 一、项目摘要

本项目基于 `nanobot` 进行二次开发，目标是在现有轻量级 Agent 框架上实现一个“能跑、能演示、能写进简历”的健身训练与饮食协同 Agent MVP。第一版不追求大而全，只聚焦 4 个核心能力：用户画像建档、周训练计划生成、每日反馈记录、动态调整建议。

项目一句话定位：

**基于 nanobot 二次开发的健身训练与饮食协同 Agent，支持用户画像建档、周训练计划生成、每日训练反馈记录和动态调整建议。**

当前仓库已有成熟的 Agent 主链路与基础能力，CLI 入口位于 `nanobot/__main__.py`，核心编排位于 `nanobot/agent/loop.py`，已有会话与长期记忆能力，适合采用“保留 nanobot 底座 + 新增业务模块”的方式推进，而不是重写一套独立系统。

## 二、MVP 范围

### 1. 用户画像建档

系统需要支持用户录入并保存以下信息：

- 性别
- 年龄
- 身高
- 体重
- 训练目标：减脂 / 增肌 / 薄肌 / 维持
- 训练经验：新手 / 初级 / 中级
- 每周可训练天数
- 单次训练时长
- 场地条件：商业健身房 / 校园健身房 / 宿舍 / 家里
- 饮食限制：无 / 食堂为主 / 外卖为主 / 忌口
- 可选补充：当前分化、薄弱部位、伤病限制

该模块的核心目标不是单纯“收集信息”，而是为 Agent 建立长期可复用的用户状态，后续的计划生成与动态调整都应基于该状态进行。

### 2. 周训练计划生成

系统根据用户画像生成一周训练安排，输出内容至少包括：

- 周几训练 / 周几休息
- 每天训练部位
- 每个动作
- 组数、次数
- 简短注意事项
- 训练日 / 休息日饮食提示

第一版只支持以下几类典型目标：

- 薄肌增肌
- 减脂保肌
- 新手入门

实现方式优先采用“规则骨架生成 + LLM 负责自然语言组织”，保证输出结构稳定，而不是完全依赖模型自由生成。

### 3. 每日反馈记录

系统支持用户每日提交训练反馈，至少包含：

- 今天是否训练
- 完成了哪些动作
- 完成度如何
- 主观疲劳等级（1-5）
- 哪个部位酸痛
- 饮食是否大致达标
- 额外备注

这一步是项目从“计划生成器”升级为“Agent”的关键，因为系统需要持续吸收用户状态变化，而不是一次性输出计划。

### 4. 动态调整建议

系统根据最近若干天的反馈记录，给出简单、可解释的调整建议。第一版采用规则驱动，LLM 仅负责组织输出措辞，不负责决定核心逻辑。

MVP 至少落地以下规则：

- 连续两次反馈疲劳较高，建议降低训练量
- 某部位酸痛明显，建议调整当天动作或改为恢复性安排
- 多次未完成计划，建议降低强度或简化动作
- 饮食连续不达标，给出更现实的饮食替代建议
- 连续多日未训练，可提示重启低强度节奏

## 三、明确不做的内容

为了避免项目发散，第一阶段不纳入以下功能：

- 复杂饮食热量精算
- 拍照识别食物
- 多模态健身动作识别
- 医学或营养诊断能力
- 社区功能
- 小程序或 App 上架
- 复杂提醒系统
- 过细的动作知识库
- 花哨 UI

第一版目标是完成一个稳定、可演示、逻辑自洽的 MVP，而不是堆砌功能点。
